import { LoginForm } from "./_components/LoginForm";

export default function LoginPage() {
  return (
    <main className="flex min-h-[calc(100dvh-12rem)] items-center justify-center bg-backghround px-6">
      <LoginForm />
    </main>
  );
}
